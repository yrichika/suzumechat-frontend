import { randomInt, randomString } from '@utils/UnsafeRandom'

describe('useJoinRequestReceiver', () => {
  beforeEach(() => {
    //
  })

  test.todo(
    'receiveJoinRequest should convert JoinRequest to ManageableJoinRequest and call addJoinRequest'
  )
})
